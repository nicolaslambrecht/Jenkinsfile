#!/usr/bin/env groovy

pipeline {
    agent any

    stages {
        stage('BuildApp') {
            steps {
                build 'BuildApp'
                echo 'Building..'
            }
        }
    }
}
